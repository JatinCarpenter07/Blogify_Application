<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
    <title>Admin - Profile Page</title>
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        .profile-card {
            max-width: 600px;
            margin: 2rem auto;
            padding: 2rem;
            background-color: #ffffff;
            border-radius: 1rem;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.05);
        }
        .profile-img {
            width: 120px;
            height: 120px;
            object-fit: cover;
            border-radius: 50%;
            border: 3px solid #0d6efd;
        }
    </style>
</head>

<body class="d-flex flex-column min-vh-100 bg-light">
    <%- include('./partials/navbar') %>

    <div class="container">
        <div class="profile-card text-center">
            <img src="<%= userProfile.profileImage || '/images/default-userProfile.png' %>" alt="Profile Image" class="profile-img mb-3">

            <h4 class="fw-bold text-primary mb-2"><%= userProfile.Name %></h4>
            <p class="mb-1 text-muted"><i class="bi bi-envelope"></i> <em><%= userProfile.email %></em></p>
            <p class="mb-1 text-muted"><i class="bi bi-lock-fill"></i> **************</p>
            <p class="mb-1 text-muted"><i class="bi bi-calendar3"></i> Registered on <%= userProfile.createdAt.toDateString() %></p>
            <p class="mb-1 text-muted"><i class="bi bi-person-badge"></i> Role: <strong><%= userProfile.role %></strong></p>
            <p class="mb-0 text-muted"><i class="bi bi-journal-text"></i> Total Blogs: <strong><%= userProfile.count || 0 %></strong></p>

            <a href="javascript:history.back()" class="btn btn-outline-secondary m-4">
                <i class="bi bi-arrow-left"></i> Back
            </a>

        </div>
    </div>

    <%- include('./partials/footer') %>
    <%- include('./partials/script') %>
</body>

</html>
